import 'package:flutter/material.dart';

class FisioterapiaPage extends StatelessWidget {
  const FisioterapiaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Fisioterapia')),
      body: const Center(child: Text('Pantalla de Fisioterapia')),
    );
  }
}